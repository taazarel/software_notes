# Factorial

The factorial [[Function]] is a function $f:\mathbb{N}\to \mathbb{N}$. It is given by assigning each natural number $n$ the product of all positive integers smaller than or equal to $n$.
- $ Denoted: $n! = 1\cdot 2 \cdot 3 \dots n$

We also define $0! = 1$.

These get big FAST.