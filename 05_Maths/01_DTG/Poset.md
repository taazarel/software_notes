# Poset

The [[Set]] S together with the [[Partial Order]] [[Relation]] R is called a partially ordered set (or "poset"). It is denoted as $(S,R)$. 
Members of S are called the "elements" of the poset.