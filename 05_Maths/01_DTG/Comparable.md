# Comparable and Incomparable Elements

The elements a,b of a [[Poset]] $(S, \preceq)$ are comparable if either $a \preceq b$ or $b \preceq a$. If this is not true, the elements are Incomparable.[^1]

[^1]: The symbol $\preceq$ is used to denote an arbitrary [[Relation]] in the poset.
